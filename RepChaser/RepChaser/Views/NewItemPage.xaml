<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="RepChaser.Views.NewItemPage"
             Title="New Item">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <ScrollView>
            <StackLayout Spacing="20" Padding="15">
                <Label Text="Exercise" FontSize="Medium" />
                <Entry Text="{Binding Item.Exercise}" FontSize="Small" />
                <Label Text="Reps per set" FontSize="Medium" />
                <Stepper Value="{Binding Item.RepsPerSet}" />
                <Label Text="Sets target daily" FontSize="Medium" />
                <Stepper Value="{Binding Item.SetsTargetDaily}" />
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>

</ContentPage>
